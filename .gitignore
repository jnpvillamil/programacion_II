# =============================================================================
# Tienda Minorista G4 | Regla de exclusión para Git / SourceTree
# =============================================================================

# -----------------------------------------------------------------------------
# Compilación automática (Maven)
# Binario .class y .jar generado en tiempo de ejecución; se reconstruye con mvn.
# -----------------------------------------------------------------------------
target/

# -----------------------------------------------------------------------------
# Configuración local del IDE (Eclipse)
# Proyecto Maven puro: estos archivo varía por máquina y genera conflicto en merge.
# -----------------------------------------------------------------------------
.classpath
.project
.settings/
.factorypath

# Metadato de espacio de trabajo Eclipse (fuera del árbol del proyecto, por precaución)
.metadata/
bin/
tmp/
*.tmp
*.bak

# -----------------------------------------------------------------------------
# Seguridad: credencial de base de datos (Aiven Cloud)
# Nunca versionar contraseña real; usar configuracion/bd.ejemplo.properties como plantilla.
# -----------------------------------------------------------------------------
configuracion/bd.properties

# -----------------------------------------------------------------------------
# Artefacto temporal del sistema operativo
# -----------------------------------------------------------------------------
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# Salida en caliente de la aplicación (bitácora y reporte financiero local)
# -----------------------------------------------------------------------------
logs/
reportes_financieros.json
